# Contributing to G-EnergySense AI

1. Fork or use the official template repository.
2. Create your feature branch (`git checkout -b feature/AmazingFeature`).
3. Commit your changes (`git commit -m 'feat: Add district telemetry feeder'`).
4. Push to the branch (`git push origin feature/AmazingFeature`).
5. Open a Pull Request.
